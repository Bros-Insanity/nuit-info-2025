---
- name: Deploy Web Container with Nginx
  hosts: localhost
  connection: local
  gather_facts: no

  vars:
    container_id: 30000
    container_name: "web-nuit-info"
    container_memory: 512
    container_cores: 1
    container_disk: 8
    container_ip: "10.0.0.20/24"
    container_gateway: "10.0.0.254"
    container_dns: "1.1.1.1"
    github_repo: "https://raw.githubusercontent.com/Bros-Insanity/nuit-info-2025/formulaire/test.html"

  tasks:
    - name: Check if container exists
      uri:
        url: "https://{{ proxmox_api_host }}:8006/api2/json/nodes/{{ proxmox_node }}/lxc/{{ container_id }}/status/current"
        method: GET
        headers:
          Authorization: "PVEAPIToken={{ proxmox_api_user }}={{ proxmox_api_token_secret }}"
        validate_certs: no
        status_code: [200, 500]
      register: container_check
      ignore_errors: yes

    - name: Stop existing container
      uri:
        url: "https://{{ proxmox_api_host }}:8006/api2/json/nodes/{{ proxmox_node }}/lxc/{{ container_id }}/status/stop"
        method: POST
        headers:
          Authorization: "PVEAPIToken={{ proxmox_api_user }}={{ proxmox_api_token_secret }}"
        validate_certs: no
        status_code: [200, 202, 500]
      when: container_check.status == 200
      ignore_errors: yes

    - name: Wait for container to stop
      pause:
        seconds: 5
      when: container_check.status == 200

    - name: Delete existing container
      uri:
        url: "https://{{ proxmox_api_host }}:8006/api2/json/nodes/{{ proxmox_node }}/lxc/{{ container_id }}"
        method: DELETE
        headers:
          Authorization: "PVEAPIToken={{ proxmox_api_user }}={{ proxmox_api_token_secret }}"
        validate_certs: no
        status_code: [200, 202, 500]
      when: container_check.status == 200
      ignore_errors: yes

    - name: Wait for container deletion
      pause:
        seconds: 5
      when: container_check.status == 200

    - name: Get current templates on storage
      uri:
        url: "https://{{ proxmox_api_host }}:8006/api2/json/nodes/{{ proxmox_node }}/storage/local/content"
        method: GET
        headers:
          Authorization: "PVEAPIToken={{ proxmox_api_user }}={{ proxmox_api_token_secret }}"
        validate_certs: no
      register: current_templates

    - name: Check if Debian standard template exists
      set_fact:
        debian_standard_exists: "{{ current_templates.json.data | selectattr('volid', 'search', 'debian-12-standard') | list | length > 0 }}"

    - name: Download Debian 12 standard template
      uri:
        url: "https://{{ proxmox_api_host }}:8006/api2/json/nodes/{{ proxmox_node }}/storage/local/download-url"
        method: POST
        headers:
          Authorization: "PVEAPIToken={{ proxmox_api_user }}={{ proxmox_api_token_secret }}"
        body_format: form-urlencoded
        body:
          url: "http://download.proxmox.com/images/system/debian-12-standard_12.7-1_amd64.tar.zst"
          content: "vztmpl"
          filename: "debian-12-standard_12.7-1_amd64.tar.zst"
        validate_certs: no
        status_code: [200, 202]
      register: download_template
      when: not debian_standard_exists

    - name: Wait for template download
      pause:
        seconds: 60
      when: not debian_standard_exists

    - name: Set template to use
      set_fact:
        existing_template:
          volid: "local:vztmpl/debian-12-standard_12.7-1_amd64.tar.zst"

    - name: Create LXC container with static IP
      uri:
        url: "https://{{ proxmox_api_host }}:8006/api2/json/nodes/{{ proxmox_node }}/lxc"
        method: POST
        headers:
          Authorization: "PVEAPIToken={{ proxmox_api_user }}={{ proxmox_api_token_secret }}"
        body_format: form-urlencoded
        body:
          vmid: "{{ container_id }}"
          hostname: "{{ container_name }}"
          ostemplate: "{{ existing_template.volid }}"
          storage: "data"
          memory: "{{ container_memory }}"
          cores: "{{ container_cores }}"
          rootfs: "data:{{ container_disk }}"
          net0: "name=eth0,bridge=vmbr0,ip={{ container_ip }},gw={{ container_gateway }}"
          nameserver: "{{ container_dns }}"
          unprivileged: 1
          start: 0
        validate_certs: no
        status_code: [200, 202]
      register: create_result

    - name: Wait for container filesystem to be ready
      pause:
        seconds: 5

    - name: Create hookscript on storage via API
      uri:
        url: "https://{{ proxmox_api_host }}:8006/api2/json/nodes/{{ proxmox_node }}/storage/local/content"
        method: POST
        headers:
          Authorization: "PVEAPIToken={{ proxmox_api_user }}={{ proxmox_api_token_secret }}"
        body_format: form-urlencoded
        body:
          content: "snippets"
          filename: "init-web-{{ container_id }}.sh"
          file: "{{ '#!/bin/bash\nif [ \"$2\" == \"post-start\" ]; then\n  sleep 10\n  pct exec $1 -- bash -c \"export DEBIAN_FRONTEND=noninteractive && apt-get update -qq && apt-get install -y nginx curl > /dev/null 2>&1 && curl -s -o /var/www/html/index.html {{ github_repo }} && systemctl enable nginx > /dev/null 2>&1 && systemctl start nginx\"\nfi' | b64encode }}"
        validate_certs: no
        status_code: [200, 202]
      register: create_hookscript
      ignore_errors: yes

    - name: Configure hookscript in container
      uri:
        url: "https://{{ proxmox_api_host }}:8006/api2/json/nodes/{{ proxmox_node }}/lxc/{{ container_id }}/config"
        method: PUT
        headers:
          Authorization: "PVEAPIToken={{ proxmox_api_user }}={{ proxmox_api_token_secret }}"
        body_format: form-urlencoded
        body:
          hookscript: "local:snippets/init-web-{{ container_id }}.sh"
        validate_certs: no
        status_code: [200, 202]
      register: config_hookscript
      ignore_errors: yes
      when: create_hookscript.rc == 0

    - name: Start container
      uri:
        url: "https://{{ proxmox_api_host }}:8006/api2/json/nodes/{{ proxmox_node }}/lxc/{{ container_id }}/status/start"
        method: POST
        headers:
          Authorization: "PVEAPIToken={{ proxmox_api_user }}={{ proxmox_api_token_secret }}"
        validate_certs: no
        status_code: [200, 202]
      register: start_result

    - name: Wait for hookscript to execute and nginx installation
      pause:
        seconds: 90

    - name: Display deployment status
      debug:
        msg: |
          Container {{ container_id }} ({{ container_name }}) deploye!
          IP: {{ container_ip }}
          Gateway: {{ container_gateway }}
          DNS: {{ container_dns }}
          Web: http://10.0.0.20/
          {% if config_hookscript.status == 200 %}
          Hookscript configure! Nginx et test.html seront installes automatiquement au demarrage.
          {% else %}
          Note: La creation du hookscript necessite un acces SSH au serveur Proxmox.
          Depuis Proxmox (10.0.0.100), executez:
          pct exec {{ container_id }} -- bash -c 'export DEBIAN_FRONTEND=noninteractive && apt-get update && apt-get install -y nginx curl && curl -o /var/www/html/index.html {{ github_repo }} && systemctl enable --now nginx'
          {% endif %}

    - name: Show result
      debug:
        msg: |
          Container {{ container_id }} ({{ container_name }}) deploye!
          IP: {{ container_ip }}
          Gateway: {{ container_gateway }}
          DNS: {{ container_dns }}
          Web: http://10.0.0.20/

